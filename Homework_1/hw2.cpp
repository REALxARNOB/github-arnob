#include <iostream>

using namespace std;

int main()
{
    //1.1.2. Write a program to enter two float numbers and find their sum (4 decimal point).
    float a, b;

    cin >> a;
    cin >> b;
    cout << "Sum = " << a + b << '\n';

    return 0;
}